# Timezone for logs and sync timestamps
TZ=Europe/Berlin

# Host port → container port 8000
PORT=47173

# Linux: UID/GID of the volume owner (optional)
UID=1000
GID=1000

# Sync interval in seconds (optional, default in code: 6h)
# REFRESH_INTERVAL_SECONDS=21600
